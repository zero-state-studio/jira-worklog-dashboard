# ============================================================================
# RAILWAY ENVIRONMENT VARIABLES
# ============================================================================
# Copy these to Railway Dashboard → Variables
# Generate secrets with: openssl rand -hex 32

# ============================================================================
# DATABASE (REQUIRED)
# ============================================================================
# Path to SQLite database file (Railway volume mounted at /app/data)
DATABASE_PATH=/app/data/worklog_storage.db

# ============================================================================
# AUTHENTICATION (REQUIRED)
# ============================================================================
# JWT Secret Key - generate with: openssl rand -hex 32
SECRET_KEY=your-64-char-hex-secret-key-here

# Google OAuth 2.0 Credentials (from Google Cloud Console)
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# OAuth Redirect URI (update with your Railway domain after deploy)
# Format: https://your-app-name.up.railway.app/api/auth/callback
GOOGLE_REDIRECT_URI=https://your-app-name.up.railway.app/api/auth/callback

# ============================================================================
# JIRA INTEGRATION (OPTIONAL - can configure via UI)
# ============================================================================
# JIRA Cloud URL
JIRA_URL=https://your-domain.atlassian.net

# JIRA User Email
JIRA_EMAIL=your-email@company.com

# JIRA API Token (from https://id.atlassian.com/manage/api-tokens)
JIRA_TOKEN=your-jira-api-token

# Tempo API Token (from https://tempo.io/settings/api-integration)
TEMPO_TOKEN=your-tempo-api-token

# ============================================================================
# APPLICATION SETTINGS (OPTIONAL)
# ============================================================================
# Environment (production, staging, development)
ENVIRONMENT=production

# Logging Level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Demo Mode (set to false for production)
JIRA_DASHBOARD_DEMO_MODE=false

# CORS Origins (comma-separated, leave empty to use defaults)
# CORS_ORIGINS=https://your-custom-domain.com

# ============================================================================
# RAILWAY AUTO-INJECTED (NO NEED TO SET)
# ============================================================================
# PORT - Railway automatically sets this (usually 8000 or dynamic)
# RAILWAY_ENVIRONMENT - production, staging, etc.
# RAILWAY_DEPLOYMENT_ID - unique deployment ID
